apiVersion: v1
kind: Secret
metadata:
  name: connections-secrets
  namespace: connections-ai
type: Opaque
stringData:
  # IMPORTANT: Replace these with real values in production!
  # Consider using SealedSecrets or external secrets management
  database_url: "postgresql+asyncpg://connections:changeme@postgresql:5432/connections"
  postgres_user: "connections"
  postgres_password: "changeme"
  jwt_secret: "your-super-secret-jwt-key-change-in-production"
