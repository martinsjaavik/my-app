apiVersion: v1
kind: Service
metadata:
  name: connections-frontend
  namespace: my-app
  labels:
    app: connections-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: connections-frontend
